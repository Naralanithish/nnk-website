// Example environment variables for the server
# Copy this file to `.env` and replace the values before running locally
MONGODB_URI=your_mongodb_connection_string_here
PORT=4000

// Example MongoDB connection string formats:
// Atlas SRV (recommended):
// MONGODB_URI="mongodb+srv://<username>:<password>@cluster0.abcd1.mongodb.net/nnk_db?retryWrites=true&w=majority"
// Standard (non-SRV):
// MONGODB_URI="mongodb://<username>:<password>@host:port/nnk_db"
